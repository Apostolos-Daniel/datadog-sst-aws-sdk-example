import { TracerWrapper } from "../../tracer-wrapper";
import { SpanContextWrapper } from "../../span-context-wrapper";
interface ContextTraceExtractor {
    extract(context: any): SpanContextWrapper | null;
}
export declare class LambdaContextTraceExtractor implements ContextTraceExtractor {
    private tracerWrapper;
    constructor(tracerWrapper: TracerWrapper);
    extract(context: any): SpanContextWrapper | null;
}
export {};
//# sourceMappingURL=lambda-context.d.ts.map