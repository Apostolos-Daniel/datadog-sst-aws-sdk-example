import { Context } from "aws-lambda";
import { TraceExtractor } from "../../trace-context-service";
import { SpanContextWrapper } from "../../span-context-wrapper";
export declare class CustomTraceExtractor {
    private customExtractor;
    constructor(customExtractor: TraceExtractor);
    extract(event: any, context: Context): Promise<SpanContextWrapper | null>;
}
//# sourceMappingURL=custom.d.ts.map