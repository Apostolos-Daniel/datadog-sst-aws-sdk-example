import { EventTraceExtractor } from "../extractor";
import { EventBridgeEvent } from "aws-lambda";
import { TracerWrapper } from "../../tracer-wrapper";
import { SpanContextWrapper } from "../../span-context-wrapper";
export declare class EventBridgeEventTraceExtractor implements EventTraceExtractor {
    private tracerWrapper;
    constructor(tracerWrapper: TracerWrapper);
    extract(event: EventBridgeEvent<any, any>): SpanContextWrapper | null;
}
//# sourceMappingURL=event-bridge.d.ts.map