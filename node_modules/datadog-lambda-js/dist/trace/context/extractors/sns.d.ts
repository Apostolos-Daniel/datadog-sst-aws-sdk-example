import { SNSEvent } from "aws-lambda";
import { TracerWrapper } from "../../tracer-wrapper";
import { EventTraceExtractor } from "../extractor";
import { SpanContextWrapper } from "../../span-context-wrapper";
export declare class SNSEventTraceExtractor implements EventTraceExtractor {
    private tracerWrapper;
    constructor(tracerWrapper: TracerWrapper);
    extract(event: SNSEvent): SpanContextWrapper | null;
}
//# sourceMappingURL=sns.d.ts.map