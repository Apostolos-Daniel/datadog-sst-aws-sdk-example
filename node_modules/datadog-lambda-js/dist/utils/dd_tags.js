"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.updateDDTags = void 0;
function updateDDTags(newTags) {
    var _a;
    if (newTags === void 0) { newTags = {}; }
    var envTags = ((_a = process.env.DD_TAGS) !== null && _a !== void 0 ? _a : "")
        .split(",")
        .filter(function (pair) { return pair.includes(":"); })
        .reduce(function (acc, pair) {
        var _a = __read(pair.split(":"), 2), key = _a[0], value = _a[1];
        if (key && value)
            acc[key] = value;
        return acc;
    }, {});
    return __assign(__assign({}, envTags), newTags);
}
exports.updateDDTags = updateDDTags;
//# sourceMappingURL=dd_tags.js.map